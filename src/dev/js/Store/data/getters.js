export const getters = {
  home (state) {
    const { home } = state
    return home
  },
  contact (state) {
    const { contact } = state
    return contact
  },
  links (state) {
    const { links } = state
    return links
  },
  projects (state) {
    const { projects } = state
    return projects
  }
}
